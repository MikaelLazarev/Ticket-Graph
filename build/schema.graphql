type TicketEvent @entity {
  id: ID!
  title: String!
  location: String!
  date: Int!
  imageUrl: String!
  price: Int!
  qty: Int!
  owner: Bytes!
  
}

type Ticket @entity {
  id: ID!
  event_id: Int!
  buyer: Bytes!
}